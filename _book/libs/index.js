/**
 * @description 云灯总集打包入口
*/
const path = require('path')
const log = require('npmlog')
const chalk = require('chalk')
const clog = console.log

log.info('Yun deng is running')

clog(chalk.blue('cli'))